import {
  findUserProfile,
  signIn,
  register,
  logOut,
  findIfUserNameExists,
  sendResetPasswordLink,
} from "./firebase";

export const fb = {
  findUserProfile,
  signIn,
  register,
  logOut,
  findIfUserNameExists,
  sendResetPasswordLink,
};

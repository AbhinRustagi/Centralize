import {
  findUserProfile,
  signIn,
  register,
  logOut,
  sendResetPasswordLink,
  sendAccountVerificationEMail,
  auth,
} from "./firebase";

export const fb = {
  findUserProfile,
  signIn,
  register,
  logOut,
  sendResetPasswordLink,
  sendAccountVerificationEMail,
  auth,
};
